<h2>LISTAR CARD</h2>
<div ng-controller="listController">
	<input type="text" name="cedula" ng-model="cedula">

	<button name="buscar" ng-click="buscarCards()">Buscar</button>

	<table border="1">
		<thead>
			<tr>
				<th>Label</th>
				<th>Moto</th>
				<th>Cedula</th>
				<th>Bono</th>
				<th>Fecha de corte</th>
				<th></th>
			</tr>
		</thead>
		<tbody ng-repeat="card in listCards">
			<tr name="card" id="tr-{{card.id}}">
				<td>{{card.label}}</td>
				<td>{{card.mount | currency:"$"}}</td>
				<td>{{card.cedula}}</td>
				<td>{{card.bonus | currency:"$"}}</td>
				<td>{{card.dateCut}}</td>
				<td><button name="eliminar" ng-click="eliminarCard(card.id)">Eliminar</button></td>
			</tr>
		</tbody>
	</table>

	<button name="new" ng-click="nuevoCard()">Nuevo</button>

</div>